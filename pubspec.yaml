name: flutter_github_feedback
description: A reusable Flutter package that sends in-app feedback (bug reports & suggestions) directly to GitHub Issues with screenshot capture.
version: 0.0.1

environment:
  sdk: ^3.7.0
  flutter: ">=3.29.0"

dependencies:
  flutter:
    sdk: flutter
  feedback: ^3.1.0
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  library:
