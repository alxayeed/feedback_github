name: feedback_github
description: A reusable Flutter package that sends in-app feedback (bug reports & suggestions) directly to GitHub Issues with screenshot capture.
version: 0.0.3

environment:
  sdk: ^3.12.0
  flutter: ">=3.44.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  # No additional Flutter-specific configuration needed for this package.
