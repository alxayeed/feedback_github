import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_github_feedback/flutter_github_feedback.dart';

void main() {
  test('library loads', () {
    // Placeholder — real tests added in TEST: commits
    expect(true, isTrue);
  });
}
